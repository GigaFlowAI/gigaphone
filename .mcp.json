{
  "mcpServers": {
    "gigaphone": {
      "command": "python3",
      "args": [
        "-m",
        "gigaphone.mcp.server"
      ],
      "env": {
        "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}/src"
      }
    }
  }
}
